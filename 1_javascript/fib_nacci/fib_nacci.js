function fib() {
    // some variables here
    var current = 0;
    var previous = 1;
    var value = 0;
    function nacci() {
        // act on variables here
        if (current == 0) && (previous == 0) {
            current = 1;
        }
        whatever = current + previous;
        value = current + previous;
        previous++;

    }
    return nacci
}

var fibCounter = fib();
fibCounter();   // 1
fibCounter();   // 1
fibCounter();   // 2
fibCounter();   // 3
fibCounter();   // 5
fibCounter();   // 8