// Basic: Make a function that is callable, has one parameter and multiplies
// the value of the parameter by 10 before returning the result. Give it the
// name multiplyByTen. Invoke it, passing it the argument 5.

function multiplyByTen(num){
    return num*10;
};

console.log(multiplyByTen(5));

// [nodemon] 1.10.2
// [nodemon] to restart at any time, enter `rs`
// [nodemon] watching: *.*
// [nodemon] starting `node build_some_functions2.js`
// 50
// [nodemon] clean exit - waiting for changes before restart
